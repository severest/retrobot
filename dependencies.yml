collectors:

- type: js-npm
  path: /
  actors:
  # pull requests for all major, minor, and patch updates
  - type: js-npm
    versions: "Y.Y.0"

- type: ruby-bundler
  path: /
  actors:
  # pull requests for all major, minor, and patch updates
  - type: ruby-bundler
    versions: "Y.Y.0"
